package main

/*
import (
	"fmt"
	"log"
	ini "gopkg.in/ini.v1"
)

func main() {
	cfg, err := ini.Load("proto.conf")
	if err!=nil{
		log.Fatalln(err)
	}
	fmt.Printf("basic :%s",cfg.Section("basic").Key("name").String())
}
*/
