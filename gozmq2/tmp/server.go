package main
/* 
import (
	"log"
	"net"
)

func init() {
	log.SetFlags(log.LUTC | log.Ldate | log.Lshortfile)

}
func main() {
	conn, err := net.Listen("tcp", "127.0.0.1:7042")
	if err != nil {
		log.Fatalln(err)
	}
	con, err := conn.Accept()
	if err != nil {
		log.Fatalln(err)
	}

	for {
		con.Write([]byte("Hello, challenger:1000\n"))

	}
}
 */